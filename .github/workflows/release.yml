name: Release
on:
  workflow_dispatch: # manual trigger only — GoReleaser config points to upstream repo
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow disabled on fork"
